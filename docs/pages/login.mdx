import Login from "../../src/Login";

# Login

La page de login est affichée automatiquement dans le cas où l’utilisateur n’est pas encore connecté, et que le composant actuel est dans un `<Auth>`.

Vous n’avez donc pas à l’appeler directement.

## Affichage

```.jsx
<Login />
```
